# ~/.config/hypr/hypridle.conf

# Bloquear a los 5 minutos (300s)
listener {
    timeout = 300
    on-timeout = "hyprlock"
    on-resume  = "notify-send 'Desbloqueado'"
}

# (Opcional) Apagar pantalla a los 10 minutos
listener {
    timeout = 600
    on-timeout = "hyprctl dispatch dpms off"
    on-resume  = "hyprctl dispatch dpms on"
}
